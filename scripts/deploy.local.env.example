# 复制为 scripts/deploy.local.env 并填写（该文件已在 .gitignore 中，不会入库）

# 云服务器地址（IP 或域名）。注意：SSH 端口不一定是 22。
DEPLOY_HOST=chengzisoft.top
DEPLOY_PORT=22

# SSH 登录用户
DEPLOY_USER=root

# SSH 私钥的绝对路径（服务器上已放好公钥）
DEPLOY_KEY=/root/.ssh/deploy_key

# 以下一般不用改
# REMOTE_DIR=/opt/minibili
# SERVICE_NAME=minibili
