// Package client defines the handler-facing service contracts (the gRPC client adapter seam).
// Local implementations are the in-process domain services; future gRPC clients must satisfy the same interfaces.
package client
